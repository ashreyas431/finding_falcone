const URL = "https://findfalcone.geektrust.com";

export const apiEndpoint = {
  planets: `${URL}/planets`,
  vehicles: `${URL}/vehicles`,
  find: `${URL}/find`,
  token: `${URL}/token`
};

export const COUNT = {
  maxDestinations: 4
};
